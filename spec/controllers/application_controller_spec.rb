require 'spec_helper'

describe ApplicationController do
  # describe "#verify_user" do
  #   context "when a user is logged in" do
  #     before { session[:id] = @tony.id }
  #
  #     it "returns true" do
  #       expect(verify_user).to be_true
  #     end
  #   end
  #
  #   context "when no user is logged in" do
  #     it "to redirect_to to the signin path" do
  #       expect(verify_user).to redirect_to sign_in_path
  #     end
  #   end
  # end
end
